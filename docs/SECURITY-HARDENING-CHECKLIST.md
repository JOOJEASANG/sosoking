# 배포 체크리스트

- Core PR 검증 작업 통과 확인
- Functions → Firestore Rules → Hosting 순서 배포 확인
- Storage Rules 수동 배포
- 이메일 인증 메일 수신 시험
- 비인증 이메일 계정의 AI 호출 차단 시험
- 신규 사건과 판결이 항상 비공개로 생성되는지 확인
- 사건 공개 시 판결문과 항소문 개인정보 재검사 시험
- 공개 판결문에 `userId`, `ownerId`, 첨부 Storage 경로가 남지 않는지 확인
- 관리자 AI 관리 탭에서 **공개 식별정보 정리** 1회 실행
- 관리자 AI 관리 탭에서 **구형 공개 ID 감사** 실행 후 확정 건수 기록
- 투표·댓글·신고·항소의 쿨다운과 일일 제한 시험
- 동일 사건 판결·항소 동시 호출 시험
- 멈춘 처리·예약 복구 버튼의 재판/예약 결과가 정상 표시되는지 확인
- 프로필 사진이 `profile-photos/{uid}/avatar.jpg`에 저장되는지 확인
- Firebase App Check와 비용 예산 알림은 콘솔에서 별도 설정
- 구형 공개 ID가 발견되면 링크·소셜 데이터 보존을 포함한 별도 마이그레이션 계획 수립