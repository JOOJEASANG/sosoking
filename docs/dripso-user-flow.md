# 드립소 사용자 흐름

1. 소소킹 하단 메뉴에서 `드립소`를 누른다.
2. `/dripso/` 독립 페이지로 이동한다.
3. 오늘의 한 줄을 읽거나 `다른 드립 뽑기`를 누른다.
4. 직장·일상·음식·디지털·관계 카테고리로 골라본다.
5. 마음에 드는 문장에 `피식`, `저장`, `복사`를 사용한다.
6. 상단 `저장함`을 눌러 저장한 문장만 다시 본다.
7. 상단 또는 하단 링크로 소소킹으로 돌아간다.
