rules_version = '2';

// 새 앱을 만들기 전의 기본 상태입니다.
// 모든 읽기/쓰기를 막아 두고, 컬렉션이 정해질 때마다 규칙을 명시적으로 추가합니다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
