name: Apply CSP cache version patch

on:
  push:
    branches: [agent/enforce-script-csp]
    paths:
      - tools/patch-csp-cache-versions.mjs

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/enforce-script-csp
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node tools/patch-csp-cache-versions.mjs
      - run: |
          node --check public/js/app.js
          node --check public/js/pages/home-court.js
          node --check public/js/pages/board-court.js
          node --check public/admin/admin-bootstrap.js
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/js/app.js public/js/pages/home-court.js public/js/pages/board-court.js public/admin/admin-bootstrap.js
          git diff --cached --quiet && exit 0
          git commit -m "Bump CSP module cache versions"
          git push origin HEAD:agent/enforce-script-csp
