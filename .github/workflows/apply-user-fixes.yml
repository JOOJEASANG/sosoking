name: Apply requested feature fixes

on:
  pull_request:
    types: [opened]

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'feature/logo-records-ai-title-humor'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply reviewed changes
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          import re

          root = Path.cwd()

          def write(path, content):
              target = root / path
              target.parent.mkdir(parents=True, exist_ok=True)
              target.write_text(content, encoding='utf-8')

          def replace_once(path, old, new):
              target = root / path
              text = target.read_text(encoding='utf-8')
              if old not in text:
                  raise RuntimeError(f'Expected block not found in {path}: {old[:100]!r}')
              target.write_text(text.replace(old, new, 1), encoding='utf-8')

          emblem = '''<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 256 256" width="256" height="256" role="img" aria-label="소소킹 판결소 로고">
  <defs>
    <linearGradient id="bg" x1="24" y1="18" x2="232" y2="238" gradientUnits="userSpaceOnUse">
      <stop stop-color="#283456"/>
      <stop offset="0.52" stop-color="#151b2e"/>
      <stop offset="1" stop-color="#090c14"/>
    </linearGradient>
    <linearGradient id="gold" x1="76" y1="38" x2="188" y2="215" gradientUnits="userSpaceOnUse">
      <stop stop-color="#ffeaa8"/>
      <stop offset="0.52" stop-color="#d4b357"/>
      <stop offset="1" stop-color="#9b6d16"/>
    </linearGradient>
  </defs>
  <rect x="4" y="4" width="248" height="248" rx="58" fill="url(#bg)"/>
  <rect x="14" y="14" width="228" height="228" rx="49" fill="none" stroke="#d4b357" stroke-width="5"/>
  <path d="M78 69 94 40l34 24 34-24 16 29-12 19H90Z" fill="url(#gold)" stroke="#ffeaa8" stroke-width="3" stroke-linejoin="round"/>
  <circle cx="128" cy="139" r="72" fill="#fff8e9" stroke="#d4b357" stroke-width="7"/>
  <text x="128" y="166" text-anchor="middle" font-family="Noto Sans KR, Apple SD Gothic Neo, sans-serif" font-size="88" font-weight="900" fill="#151b2e">소</text>
  <g transform="translate(177 178) rotate(-12)">
    <rect x="-37" y="-12" width="74" height="24" rx="8" fill="url(#gold)" stroke="#ffeaa8" stroke-width="2"/>
    <rect x="27" y="6" width="14" height="54" rx="7" fill="url(#gold)" transform="rotate(-38 34 33)"/>
  </g>
  <circle cx="192" cy="194" r="35" fill="#c83c32" stroke="#fff8e9" stroke-width="5"/>
  <text x="192" y="205" text-anchor="middle" font-family="Noto Serif KR, serif" font-size="31" font-weight="900" fill="#fff8e9">판</text>
</svg>
'''
          write('public/logo.svg', emblem)
          write('public/app-icon.svg', emblem)

          home = root / 'public/js/pages/home.js'
          home_text = home.read_text(encoding='utf-8')
          old_logo = '''        <img src="/app-icon.svg?v=20260630-3" alt="소소킹 로고" style="width:92px;height:92px;margin-bottom:14px;display:block;margin-left:auto;margin-right:auto;animation:wiggle 3.5s ease-in-out infinite;" />'''
          new_logo = '''        <style>
          @keyframes logoFloat { 0%,100% { transform:translateY(0); } 50% { transform:translateY(-5px); } }
          .hero-logo-mark { width:112px;height:112px;margin:0 auto 16px;display:block;animation:logoFloat 3.2s ease-in-out infinite;filter:none;image-rendering:auto; }
          @media (prefers-reduced-motion: reduce) { .hero-logo-mark { animation:none; } }
        </style>
        <img src="/logo.svg?v=20260727-1" alt="왕관과 판결봉이 있는 소소킹 판결소 로고" class="hero-logo-mark" />'''
          if old_logo not in home_text:
              raise RuntimeError('Home logo block not found')
          home_text = home_text.replace(old_logo, new_logo, 1)
          home_text = home_text.replace('사건명과 경위를 적습니다. 실명 대신 억울함만 제출하세요.', '사건 경위만 적으면 AI 재판부가 사건명을 거창하게 붙입니다. 실명 대신 억울함만 제출하세요.', 1)

          old_feed = '''async function _loadPublicFeed() {
  try {
    const snap = await getDocs(query(collection(db, 'results'), where('isPublic', '==', true), orderBy('createdAt', 'desc'), limit(20)));
    if (snap.empty) return;
    _feedAll = snap.docs.map(d => [d.id, d.data()]);
    const feedEl = document.getElementById('feed-container');
    if (!feedEl) return;
    feedEl.innerHTML = _feedAll.map(([id, r]) => _caseCard(id, r)).join('');
    _updatePopularJudge();
    const searchEl = document.getElementById('feed-search');
    if (searchEl) searchEl.addEventListener('input', _applySearch);
  } catch {
    // Keep example fallback
  }
}'''
          new_feed = '''function _createdMs(record) {
  const value = record?.createdAt;
  if (!value) return 0;
  if (typeof value.toMillis === 'function') return value.toMillis();
  if (typeof value.toDate === 'function') return value.toDate().getTime();
  const parsed = new Date(value).getTime();
  return Number.isFinite(parsed) ? parsed : 0;
}

async function _publicResultRows(maxRows) {
  try {
    const ordered = await getDocs(query(collection(db, 'results'), where('isPublic', '==', true), orderBy('createdAt', 'desc'), limit(maxRows)));
    return ordered.docs.map(d => [d.id, d.data()]);
  } catch (err) {
    console.warn('ordered public result query unavailable; using client sort', err);
    const fallback = await getDocs(query(collection(db, 'results'), where('isPublic', '==', true)));
    return fallback.docs
      .map(d => [d.id, d.data()])
      .sort((a, b) => _createdMs(b[1]) - _createdMs(a[1]))
      .slice(0, maxRows);
  }
}

async function _loadPublicFeed() {
  try {
    _feedAll = await _publicResultRows(20);
    if (_feedAll.length === 0) return;
    const feedEl = document.getElementById('feed-container');
    if (!feedEl) return;
    feedEl.innerHTML = _feedAll.map(([id, r]) => _caseCard(id, r)).join('');
    _updatePopularJudge();
    const searchEl = document.getElementById('feed-search');
    if (searchEl) searchEl.addEventListener('input', _applySearch);
  } catch (err) {
    console.error('public feed load failed', err);
  }
}'''
          if old_feed not in home_text:
              raise RuntimeError('Home public feed block not found')
          home.write_text(home_text.replace(old_feed, new_feed, 1), encoding='utf-8')

          board = '''import { db } from '../firebase.js?v=20260630-3';
import { collection, query, where, orderBy, limit, getDocs } from 'https://www.gstatic.com/firebasejs/12.12.0/firebase-firestore.js';
import { escapeHtml, compactText } from '../utils/sanitize.js?v=20260630-3';

const JUDGE_ICON = {
  '엄벌주의형':'👨‍⚖️','감성형':'🥹','현실주의형':'🤦','과몰입형':'🔥','피곤형':'😴','논리집착형':'🧮','드립형':'🎭'
};

function fmtDate(ts) {
  if (!ts) return '';
  const d = ts.toDate ? ts.toDate() : new Date(ts);
  return d.toLocaleString('ko-KR', { timeZone: 'Asia/Seoul', month: 'numeric', day: 'numeric', hour: '2-digit', minute: '2-digit' });
}

function createdMs(record) {
  const value = record?.createdAt;
  if (!value) return 0;
  if (typeof value.toMillis === 'function') return value.toMillis();
  if (typeof value.toDate === 'function') return value.toDate().getTime();
  const parsed = new Date(value).getTime();
  return Number.isFinite(parsed) ? parsed : 0;
}

function totalVotes(r) {
  return Number(r.reactionTotal || r.totalVotes || 0);
}
function totalComments(r) {
  return Number(r.commentCount || 0);
}

async function loadPublicRows(maxRows = 40) {
  try {
    const ordered = await getDocs(query(collection(db, 'results'), where('isPublic', '==', true), orderBy('createdAt', 'desc'), limit(maxRows)));
    return ordered.docs.map(d => [d.id, d.data()]);
  } catch (err) {
    console.warn('ordered board query unavailable; using client sort', err);
    const fallback = await getDocs(query(collection(db, 'results'), where('isPublic', '==', true)));
    return fallback.docs
      .map(d => [d.id, d.data()])
      .sort((a, b) => createdMs(b[1]) - createdMs(a[1]))
      .slice(0, maxRows);
  }
}

export async function renderBoard(container) {
  container.innerHTML = `
    <div>
      <div class="page-header"><a href="#/" class="back-btn">‹</a><span class="logo">판결기록</span></div>
      <div class="container" style="padding-top:22px;padding-bottom:90px;">
        <div style="margin-bottom:18px;">
          <div style="font-family:var(--font-serif);font-size:22px;font-weight:900;color:var(--gold);margin-bottom:6px;">공개 판결기록</div>
          <div style="font-size:13px;color:var(--cream-dim);line-height:1.7;">다른 사람들이 공개한 생활판결 기록입니다. 판결문을 열람하고 원고 편·피고 편 투표와 방청석 한마디를 남길 수 있습니다.</div>
        </div>
        <div id="today-pick"></div>
        <div id="board-list"><div class="loading-dots"><span></span><span></span><span></span></div></div>
      </div>
    </div>`;

  const list = document.getElementById('board-list');
  try {
    const rows = await loadPublicRows(40);
    if (rows.length === 0) {
      list.innerHTML = `<div style="text-align:center;padding:52px 0;color:var(--cream-dim);"><div style="font-size:46px;margin-bottom:12px;">📭</div>아직 공개된 판결기록이 없습니다.<br><a href="#/submit" style="color:var(--gold);margin-top:12px;display:inline-block;">첫 사건 접수하기</a></div>`;
      return;
    }
    const top = [...rows].sort((a, b) => (totalVotes(b[1]) + totalComments(b[1])) - (totalVotes(a[1]) + totalComments(a[1])))[0];
    document.getElementById('today-pick').innerHTML = top ? todayPick(top) : '';
    list.innerHTML = `<div style="font-size:13px;color:var(--cream-dim);margin:18px 0 8px;">📜 최근 공개 판결기록</div><div style="display:flex;flex-direction:column;gap:10px;">${rows.map(row => boardRow(...row)).join('')}</div>`;
  } catch (err) {
    console.error(err);
    list.innerHTML = `<div style="text-align:center;padding:52px 0;color:var(--cream-dim);">판결기록을 불러오지 못했습니다.<br><span style="font-size:12px;opacity:.7;">${escapeHtml(err.message || '')}</span></div>`;
  }
}

function todayPick([id, r]) {
  const icon = JUDGE_ICON[r.judgeType] || '⚖️';
  return `<div class="card" onclick="location.hash='#/result/${encodeURIComponent(id)}'" style="padding:20px;margin-bottom:16px;cursor:pointer;border-color:rgba(201,168,76,.65);background:linear-gradient(135deg,rgba(201,168,76,.12),rgba(255,255,255,.03));">
    <div style="font-size:12px;color:var(--gold);font-weight:900;letter-spacing:.12em;margin-bottom:8px;">오늘의 판결기록</div>
    <div style="font-family:var(--font-serif);font-size:21px;font-weight:900;line-height:1.45;margin-bottom:8px;">${escapeHtml(r.caseTitle || '제목 없음')}</div>
    <div style="font-size:14px;color:var(--cream-dim);line-height:1.65;margin-bottom:12px;">${escapeHtml(compactText(r.sentence || r.verdict || '', 96))}</div>
    <div style="display:flex;justify-content:space-between;gap:8px;font-size:12px;color:var(--cream-dim);"><span>${icon} ${escapeHtml(r.judgeType || 'AI')} 판사</span><span>🧑‍⚖️ ${totalVotes(r)}표 · 💬 ${totalComments(r)}</span></div>
  </div>`;
}

function boardRow(id, r) {
  const icon = JUDGE_ICON[r.judgeType] || '⚖️';
  const isDaily = r.source === 'daily_ai';
  return `<div class="card" onclick="location.hash='#/result/${encodeURIComponent(id)}'" style="padding:16px 18px;cursor:pointer;">
    <div style="display:flex;justify-content:space-between;align-items:flex-start;gap:10px;margin-bottom:6px;"><div style="font-weight:800;font-size:15px;line-height:1.45;flex:1;">${escapeHtml(r.caseTitle || '제목 없음')}</div><div style="font-size:11px;color:var(--cream-dim);white-space:nowrap;margin-top:2px;">${escapeHtml(fmtDate(r.createdAt))}</div></div>
    <div style="font-size:13px;color:var(--cream-dim);line-height:1.6;margin-bottom:10px;">${escapeHtml(compactText(r.sentence || r.verdict || '', 86))}</div>
    <div style="display:flex;justify-content:space-between;align-items:center;gap:8px;font-size:12px;"><span style="color:var(--cream-dim);">${icon} ${escapeHtml(r.judgeType || 'AI')} 판사 · 억울지수 ${escapeHtml(r.grievanceIndex || '?')}/10</span><span style="color:var(--gold);white-space:nowrap;">🧑‍⚖️ ${totalVotes(r)} · 💬 ${totalComments(r)} →</span></div>
    ${isDaily ? `<div style="margin-top:8px;font-size:11px;color:var(--gold);font-weight:800;">오늘의 AI 사건</div>` : ''}
  </div>`;
}
'''
          write('public/js/pages/board.js', board)

          submit = root / 'public/js/pages/submit.js'
          submit_text = submit.read_text(encoding='utf-8')
          submit_text = submit_text.replace('const MAX_TITLE = 30;\n', '', 1)
          title_block = '''          <div class="form-group">
            <label class="form-label">사건명 <span style="color:var(--red)">*</span></label>
            <input type="text" id="case-title" class="form-input" maxlength="${MAX_TITLE}" placeholder="예: 라면 국물 무단 음용 사건" required>
            <div class="char-counter"><span id="title-count">0</span>/${MAX_TITLE}</div>
          </div>
'''
          ai_title_block = '''          <div class="card" style="padding:14px;margin-bottom:18px;background:rgba(201,168,76,.08);border-color:rgba(201,168,76,.32);">
            <div style="font-weight:900;color:var(--gold);margin-bottom:5px;">🤖 사건명은 AI 재판부가 자동 작성</div>
            <div style="font-size:12px;color:var(--cream-dim);line-height:1.65;">사건 경위를 읽고 짧고 거창한 생활법정 사건명으로 바꿉니다. 예: ‘마지막 만두 단독 집행 사건’</div>
          </div>
'''
          if title_block not in submit_text:
              raise RuntimeError('Submit title block not found')
          submit_text = submit_text.replace(title_block, ai_title_block, 1)
          listener = '''  document.getElementById('case-title').addEventListener('input', function() {
    document.getElementById('title-count').textContent = this.value.length;
  });
'''
          submit_text = submit_text.replace(listener, '', 1)
          submit_text = submit_text.replace("    const title = document.getElementById('case-title').value.trim();\n", '', 1)
          submit_text = submit_text.replace("    if (!title || !desc) return showToast('사건명과 사건 경위를 입력해주세요.', 'error');", "    if (!desc) return showToast('사건 경위를 입력해주세요.', 'error');", 1)
          submit_text = submit_text.replace("    if (_isTooSerious(`${title} ${desc}`)) {", "    if (_isTooSerious(desc)) {", 1)
          submit_text = submit_text.replace("    btn.textContent = '전자소송 접수 중...';", "    btn.textContent = 'AI 사건명 작성 후 접수 중...';", 1)
          submit_text = submit_text.replace("      const res = await submitCase({ caseTitle: title, caseDescription: desc, grievanceIndex: grievance, desiredVerdict: desired, selectedJudge, isPublic });", "      const res = await submitCase({ caseDescription: desc, grievanceIndex: grievance, desiredVerdict: desired, selectedJudge, isPublic });", 1)
          submit.write_text(submit_text, encoding='utf-8')

          submit_secure = '''const { onCall, HttpsError } = require('firebase-functions/v2/https');
const { getFirestore, FieldValue } = require('firebase-admin/firestore');

const db = getFirestore();
const REGION = 'asia-northeast3';
const MAX_DESC = 200;
const MAX_DESIRED = 100;
const HARD_DAILY_LIMIT = 3;
const DEFAULT_COOLDOWN_SEC = 45;
const JUDGES = ['엄벌주의형','감성형','현실주의형','과몰입형','피곤형','논리집착형','드립형'];
const NICK_ADJ = ['억울한','분노한','황당한','지친','당황한','슬픈','안타까운','기막힌'];
const NICK_NOUN = ['직장인','집사','아무개','라면러버','과자지킴이','충전기수호자','리모컨분실자','냉장고파수꾼'];

function requireRealLogin(request) {
  if (!request.auth) throw new HttpsError('unauthenticated', '로그인이 필요합니다.');
  const provider = request.auth.token.firebase?.sign_in_provider || '';
  if (provider === 'anonymous') {
    throw new HttpsError('unauthenticated', '사건 접수는 구글 또는 이메일 로그인 후 이용할 수 있습니다.');
  }
}
function textValue(value, maxLen) {
  return String(value || '').replace(/[\\u0000-\\u001F\\u007F]/g, ' ').replace(/\\s+/g, ' ').trim().slice(0, maxLen);
}
function clampNumber(value, fallback, min, max) {
  const n = Number(value);
  if (!Number.isFinite(n)) return fallback;
  return Math.max(min, Math.min(max, Math.floor(n)));
}
function boolValue(value, fallback = false) {
  if (typeof value === 'boolean') return value;
  if (value === 'true') return true;
  if (value === 'false') return false;
  return fallback;
}
function kstDateKey(date = new Date()) {
  return new Intl.DateTimeFormat('sv-SE', { timeZone: 'Asia/Seoul', year: 'numeric', month: '2-digit', day: '2-digit' }).format(date);
}
function makeDocket(today) {
  const compact = today.replace(/-/g, '').slice(2);
  return `소소${compact}-생활판결-${Math.floor(1000 + Math.random() * 9000)}`;
}
function randomNickname() {
  return NICK_ADJ[Math.floor(Math.random() * NICK_ADJ.length)] + NICK_NOUN[Math.floor(Math.random() * NICK_NOUN.length)];
}
function selectedJudgeOrBlank(value) { return JUDGES.includes(value) ? value : ''; }
function containsBannedWord(text, bannedWords = []) {
  const source = String(text || '').toLowerCase();
  return bannedWords.some(word => {
    const w = String(word || '').trim().toLowerCase();
    return w && source.includes(w);
  });
}
async function loadSettings() {
  const snap = await db.doc('site_settings/config').get();
  return snap.exists ? snap.data() : {};
}
async function loadUserNickname(uid) {
  try {
    const snap = await db.doc(`users/${uid}`).get();
    if (!snap.exists) return '';
    return textValue(snap.data().nickname, 30);
  } catch (e) {
    console.error('profile load failed:', e);
    return '';
  }
}

exports.submitCase = onCall({ region: REGION, timeoutSeconds: 30, memory: '256MiB' }, async request => {
  requireRealLogin(request);

  const uid = request.auth.uid;
  const data = request.data || {};
  const desc = textValue(data.caseDescription, MAX_DESC);
  const desired = textValue(data.desiredVerdict, MAX_DESIRED);
  const grievance = clampNumber(data.grievanceIndex, 5, 1, 10);
  const selectedJudge = selectedJudgeOrBlank(data.selectedJudge);
  const isPublic = boolValue(data.isPublic, true);
  const profileNickname = await loadUserNickname(uid);

  if (!desc) throw new HttpsError('invalid-argument', '사건 경위를 입력해주세요.');

  const settings = await loadSettings();
  const cooldownSec = clampNumber(settings.cooldownSec, DEFAULT_COOLDOWN_SEC, 0, 300);
  const bannedWords = Array.isArray(settings.bannedWords) ? settings.bannedWords : [];
  if (containsBannedWord(`${desc} ${desired}`, bannedWords)) {
    throw new HttpsError('failed-precondition', '관리자가 제한한 단어가 포함되어 있습니다.');
  }

  const today = kstDateKey();
  const docketNumber = makeDocket(today);
  const caseId = `${uid}_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
  const caseRef = db.doc(`cases/${caseId}`);
  const limitRef = db.doc(`rate_limits/${uid}`);

  await db.runTransaction(async tx => {
    const limitSnap = await tx.get(limitRef);
    const current = limitSnap.exists ? limitSnap.data() : {};
    const count = current.date === today ? Number(current.count || 0) : 0;
    if (count >= HARD_DAILY_LIMIT) throw new HttpsError('resource-exhausted', '오늘 접수 한도 3건을 초과했습니다.');
    if (current.lastSubmittedAt) {
      const lastMs = current.lastSubmittedAt.toMillis ? current.lastSubmittedAt.toMillis() : new Date(current.lastSubmittedAt).getTime();
      const diffSec = Math.floor((Date.now() - lastMs) / 1000);
      if (cooldownSec > 0 && diffSec < cooldownSec) {
        throw new HttpsError('resource-exhausted', `${cooldownSec - diffSec}초 후에 다시 접수할 수 있습니다.`);
      }
    }

    tx.set(caseRef, {
      userId: uid,
      docketNumber,
      courtName: '소소킹 판결소',
      courtroom: '제404호 생활법정',
      division: '제3생활부',
      courtStage: 'filed',
      caseTitle: 'AI 사건명 작성 중',
      caseDescription: desc,
      grievanceIndex: grievance,
      nickname: profileNickname || randomNickname(),
      desiredVerdict: desired,
      selectedJudge,
      status: 'pending',
      isPublic,
      reportCount: 0,
      createdAt: FieldValue.serverTimestamp(),
      updatedAt: FieldValue.serverTimestamp()
    });
    tx.set(limitRef, { date: today, count: count + 1, lastSubmittedAt: FieldValue.serverTimestamp(), updatedAt: FieldValue.serverTimestamp() }, { merge: true });
  });

  return { caseId, docketNumber, dailyLimit: HARD_DAILY_LIMIT };
});
'''
          write('functions/submit-secure.js', submit_secure)

          generate = '''const { onCall, HttpsError } = require('firebase-functions/v2/https');
const { getFirestore, FieldValue } = require('firebase-admin/firestore');
const { defineSecret } = require('firebase-functions/params');
const { GoogleGenerativeAI } = require('@google/generative-ai');

const db = getFirestore();
const geminiKey = defineSecret('GEMINI_API_KEY');
const REGION = 'asia-northeast3';
const JUDGES = ['엄벌주의형','감성형','현실주의형','과몰입형','피곤형','논리집착형','드립형'];
const SENTENCE_FALLBACKS = [
  '피고는 3일간 간식 선택권을 원고에게 우선 배정한다.',
  '피고는 다음 실수 때 변명보다 사과를 먼저 제출한다.',
  '피고는 하루 동안 생활법정의 눈치를 성실히 살핀다.',
  '피고는 다음 간식 구매 시 원고 몫을 1.5배로 확보한다.'
];

function cleanText(value, maxLen) {
  return String(value || '').replace(/[\\u0000-\\u001F\\u007F]/g, ' ').replace(/\\s+/g, ' ').trim().slice(0, maxLen);
}
function normalizeCaseTitle(value, description = '') {
  let title = cleanText(value, 30).replace(/["“”'`]/g, '').replace(/[.!?]+$/g, '').trim();
  if (!title || title === 'AI 사건명 작성 중') {
    const base = cleanText(description, 80)
      .replace(/^(오늘|어제|방금|아까)\\s*/g, '')
      .replace(/[.!?].*$/g, '')
      .replace(/(했어요|했습니다|했다|해요|합니다)$/g, '')
      .trim()
      .slice(0, 18);
    title = base || '정체불명 생활 억울';
  }
  if (!title.endsWith('사건')) title = `${title} 사건`;
  return cleanText(title, 30);
}
function pickJudge(value) {
  if (JUDGES.includes(value)) return value;
  const seed = Date.now() + Math.floor(Math.random() * 1000000);
  return JUDGES[seed % JUDGES.length];
}
function kstDateKey(date = new Date()) {
  return new Intl.DateTimeFormat('sv-SE', { timeZone: 'Asia/Seoul', year: 'numeric', month: '2-digit', day: '2-digit' }).format(date);
}
function oneSentence(text) {
  let s = cleanText(text, 110).replace(/["“”'`]/g, '').trim().split(/\\n/)[0].trim();
  if (!s) s = SENTENCE_FALLBACKS[Math.floor(Math.random() * SENTENCE_FALLBACKS.length)];
  if (!s.startsWith('피고는')) s = `피고는 ${s.replace(/^피고(인)?은?\\s*/, '')}`;
  if (!s.endsWith('.')) s += '.';
  return s.length > 68 ? SENTENCE_FALLBACKS[Math.floor(Math.random() * SENTENCE_FALLBACKS.length)] : s;
}
function safeJson(text) {
  const raw = String(text || '').replace(/```json|```/g, '').trim();
  const start = raw.indexOf('{');
  const end = raw.lastIndexOf('}');
  if (start < 0 || end < start) throw new Error('JSON parse failed');
  return JSON.parse(raw.slice(start, end + 1));
}
function fallback(c, judgeType) {
  const caseTitle = normalizeCaseTitle('', c.caseDescription);
  return {
    caseTitle,
    reception: `${caseTitle}은 별일 아닌 듯 보였으나 원고의 표정이 이미 대법정급이어서 제404호 생활법정에 긴급 접수되었다. 서기는 사건기록 첫 장에 ‘일단 웃음 참기’라고 적었다.`,
    investigation: '조사관은 현장에 남은 빈자리, 미묘한 눈빛, 뒤늦게 나온 변명 한 스푼을 증거로 채택했다. 특히 아무도 요구하지 않은 해명은 유죄 방향으로 매우 성실하게 작용했다.',
    plaintiffArg: '원고는 문제의 크기보다 서운함의 밀도가 높다고 주장했다. 원고 측은 “사소한 일이면 사과도 사소하게 빨리 하면 됐습니다”라는 결정적 문장을 제출했다.',
    defendantArg: '피고는 그런 뜻이 아니었다며 생활형 선처를 구했다. 그러나 재판부는 뜻이 아니었다는 말이 사건 발생 후에만 등장한 점을 엄숙하게 의심했다.',
    verdict: `${judgeType} 판사는 본 사건이 국가적 위기는 아니지만 원고의 저녁 기분에는 분명한 재난이었다고 판단한다. 재판부는 피고의 반성문보다 다음 행동을 더 믿기로 하며, 본 판결은 실제 법적 효력이 없는 오락 콘텐츠임을 다시 한 번 쓸데없이 엄숙하게 고지한다.`,
    sentence: SENTENCE_FALLBACKS[Math.floor(Math.random() * SENTENCE_FALLBACKS.length)]
  };
}
async function loadSettings() {
  const snap = await db.doc('site_settings/config').get();
  return snap.exists ? snap.data() : {};
}
async function resetStaleCase(caseRef, c, resultRef) {
  if (c.status === 'completed') {
    const existing = await resultRef.get();
    if (existing.exists) return { skip: true, reason: 'completed' };
    await caseRef.update({ status: 'pending', courtStage: 'filed', errorMessage: FieldValue.delete(), updatedAt: FieldValue.serverTimestamp() });
    return { skip: false, data: { ...c, status: 'pending', courtStage: 'filed' } };
  }
  if (c.status === 'processing') {
    const started = c.processingStartedAt?.toMillis ? c.processingStartedAt.toMillis() : 0;
    if (started && Date.now() - started < 10 * 60 * 1000) return { skip: true, reason: 'processing' };
    await caseRef.update({ status: 'pending', courtStage: 'filed', errorMessage: FieldValue.delete(), updatedAt: FieldValue.serverTimestamp() });
    return { skip: false, data: { ...c, status: 'pending', courtStage: 'filed' } };
  }
  return { skip: false, data: c };
}

exports.generateTrial = onCall({ region: REGION, secrets: [geminiKey], timeoutSeconds: 300, memory: '512MiB' }, async request => {
  if (!request.auth) throw new HttpsError('unauthenticated', '로그인이 필요합니다.');

  const uid = request.auth.uid;
  const caseId = cleanText(request.data?.caseId, 180);
  if (!caseId) throw new HttpsError('invalid-argument', 'caseId required');

  const caseRef = db.doc(`cases/${caseId}`);
  const resultRef = db.doc(`results/${caseId}`);
  const caseSnap = await caseRef.get();
  if (!caseSnap.exists) throw new HttpsError('not-found', '사건을 찾을 수 없습니다.');

  let c = caseSnap.data();
  if (c.userId !== uid) throw new HttpsError('permission-denied', '본인 사건만 재판할 수 있습니다.');
  const reset = await resetStaleCase(caseRef, c, resultRef);
  if (reset.skip) return { success: true, skipped: reset.reason };
  c = reset.data;
  if (c.status !== 'pending') throw new HttpsError('failed-precondition', '처리할 수 없는 사건 상태입니다.');

  const judgeType = pickJudge(c.selectedJudge);
  await db.runTransaction(async tx => {
    const fresh = await tx.get(caseRef);
    if (!fresh.exists) throw new HttpsError('not-found', '사건을 찾을 수 없습니다.');
    const current = fresh.data();
    if (current.userId !== uid) throw new HttpsError('permission-denied', '본인 사건만 재판할 수 있습니다.');
    if (current.status !== 'pending') return;
    c = current;
    tx.update(caseRef, {
      status: 'processing',
      courtStage: 'hearing',
      judgeType,
      processingStartedAt: FieldValue.serverTimestamp(),
      errorMessage: FieldValue.delete(),
      updatedAt: FieldValue.serverTimestamp()
    });
  });

  const latest = await caseRef.get();
  if (latest.data()?.status !== 'processing') return { success: true, skipped: latest.data()?.status || 'unknown' };

  const isPublic = c.isPublic !== false;
  const settings = await loadSettings();
  const modelName = cleanText(settings.geminiModel, 60) || 'gemini-2.5-flash';
  let data = fallback(c, judgeType);
  let totals = { requests: 0, inputTokens: 0, outputTokens: 0 };

  try {
    const model = new GoogleGenerativeAI(geminiKey.value().trim()).getGenerativeModel({
      model: modelName,
      generationConfig: { temperature: 1.08, topP: 0.95, maxOutputTokens: 2200, responseMimeType: 'application/json' }
    });
    const prompt = `당신은 소소킹 판결소의 코미디 작가 겸 생활법정 재판부다. 사용자 사건을 실제 법률 판단이 아닌 안전한 오락 콘텐츠로 작성한다.

사건 경위: ${cleanText(c.caseDescription, 200)}
억울지수: ${Number(c.grievanceIndex || 5)}/10
원하는 처분: ${cleanText(c.desiredVerdict, 100) || '없음'}
담당 판사: ${judgeType}

유머 규칙:
1. 별것 아닌 일을 국가적 위기처럼 엄숙하게 다루되 욕설·비하·혐오 표현은 쓰지 않는다.
2. 사건 경위에서 핵심을 뽑아 10~24자의 간결한 caseTitle을 만든다. 반드시 ‘사건’으로 끝낸다.
3. reception에는 쓸데없이 장엄한 접수 사유와 서기의 짧은 속마음 농담을 넣는다.
4. investigation에는 하찮은 증거 2~3개를 과학수사처럼 과대평가한다.
5. plaintiffArg와 defendantArg에는 양측이 말이 되는 듯 안 되는 듯 진지하게 다투는 문장을 넣는다.
6. verdict에는 판사의 근엄한 헛소리 또는 생활형 명언을 최소 1개 넣는다.
7. sentence는 반드시 ‘피고는 ...한다.’ 한 문장이고, 실제로 실행 가능하지만 웃긴 처분으로 만든다.
8. 각 필드는 서로 다른 농담을 사용하며 같은 표현을 반복하지 않는다.
9. 실명·연락처·주민번호처럼 보이는 정보는 반복하지 않는다.
10. 실제 법률 자문이나 법적 효력이 없다는 점을 verdict에 자연스럽게 포함한다.

반드시 JSON만 출력한다. 필드: caseTitle, reception, investigation, plaintiffArg, defendantArg, verdict, sentence.`;
    const result = await model.generateContent(prompt);
    const meta = result.response.usageMetadata || {};
    totals = {
      requests: 1,
      inputTokens: meta.promptTokenCount || 0,
      outputTokens: meta.candidatesTokenCount || 0
    };
    const parsed = safeJson(result.response.text());
    data = {
      caseTitle: normalizeCaseTitle(parsed.caseTitle, c.caseDescription),
      reception: cleanText(parsed.reception, 760) || data.reception,
      investigation: cleanText(parsed.investigation, 760) || data.investigation,
      plaintiffArg: cleanText(parsed.plaintiffArg, 760) || data.plaintiffArg,
      defendantArg: cleanText(parsed.defendantArg, 760) || data.defendantArg,
      verdict: cleanText(parsed.verdict, 1400) || data.verdict,
      sentence: oneSentence(parsed.sentence || data.sentence)
    };
  } catch (err) {
    console.error('generateTrial AI failed, using fallback:', err);
  }

  const finalTitle = normalizeCaseTitle(data.caseTitle, c.caseDescription);
  try {
    const batch = db.batch();
    batch.set(resultRef, {
      source: 'user',
      userId: uid,
      isPublic,
      docketNumber: c.docketNumber || '',
      courtName: '소소킹 판결소',
      courtroom: '제404호 생활법정',
      division: '제3생활부',
      caseTitle: finalTitle,
      caseDescription: c.caseDescription || '',
      grievanceIndex: c.grievanceIndex || 5,
      nickname: c.nickname || '익명 원고',
      desiredVerdict: c.desiredVerdict || '',
      judgeType,
      reception: data.reception,
      investigation: data.investigation,
      plaintiffArg: data.plaintiffArg,
      defendantArg: data.defendantArg,
      verdict: data.verdict,
      sentence: data.sentence,
      reactionTotal: 0,
      commentCount: 0,
      courtStage: 'sentenced',
      createdAt: c.createdAt || FieldValue.serverTimestamp(),
      updatedAt: FieldValue.serverTimestamp()
    }, { merge: true });
    batch.update(caseRef, {
      caseTitle: finalTitle,
      status: 'completed',
      courtStage: 'sentenced',
      judgeType,
      isPublic,
      completedAt: FieldValue.serverTimestamp(),
      updatedAt: FieldValue.serverTimestamp(),
      processingStartedAt: FieldValue.delete(),
      errorMessage: FieldValue.delete()
    });
    await batch.commit();
  } catch (err) {
    await caseRef.update({ status: 'error', courtStage: 'error', errorMessage: err.message || '알 수 없는 오류', updatedAt: FieldValue.serverTimestamp() }).catch(() => null);
    throw err;
  } finally {
    try {
      const today = kstDateKey();
      await db.doc(`usage_stats/daily_${today}`).set({
        date: today,
        geminiRequests: FieldValue.increment(totals.requests),
        geminiInputTokens: FieldValue.increment(totals.inputTokens),
        geminiOutputTokens: FieldValue.increment(totals.outputTokens),
        caseCount: FieldValue.increment(1),
        firestoreReads: FieldValue.increment(4),
        firestoreWrites: FieldValue.increment(3),
        functionInvocations: FieldValue.increment(1),
        updatedAt: FieldValue.serverTimestamp()
      }, { merge: true });
    } catch (e) {
      console.error('usage log failed:', e);
    }
  }

  return { success: true, judgeType, isPublic, caseTitle: finalTitle };
});
'''
          write('functions/generate-trial-lite.js', generate)

          replace_once('.github/workflows/firebase-deploy.yml', '      - name: Deploy Firestore rules and Hosting\n', '      - name: Deploy Firestore indexes, rules and Hosting\n')
          replace_once('.github/workflows/firebase-deploy.yml', 'firebase deploy --only firestore:rules,hosting --project sosoking-481e6 --non-interactive', 'firebase deploy --only firestore:indexes,firestore:rules,hosting --project sosoking-481e6 --non-interactive')

          index_path = root / 'public/index.html'
          index_text = index_path.read_text(encoding='utf-8').replace('v=20260630-8', 'v=20260727-1')
          index_path.write_text(index_text, encoding='utf-8')

          manifest_path = root / 'public/site.webmanifest'
          manifest_text = manifest_path.read_text(encoding='utf-8').replace('/app-icon.svg?v=20260630-8', '/app-icon.svg?v=20260727-1')
          manifest_path.write_text(manifest_text, encoding='utf-8')

          sw_path = root / 'public/sw.js'
          sw_text = sw_path.read_text(encoding='utf-8').replace("const CACHE_NAME = 'sosoking-app-v20260630-22';", "const CACHE_NAME = 'sosoking-app-v20260727-1';")
          sw_path.write_text(sw_text, encoding='utf-8')

          workflow = root / '.github/workflows/apply-user-fixes.yml'
          if workflow.exists():
              workflow.unlink()
          PY

      - name: Install Functions dependencies
        run: npm ci --prefix functions --no-audit --no-fund

      - name: Validate changes
        run: npm run check

      - name: Commit changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "OpenAI"
          git config user.email "noreply@openai.com"
          git add -A
          git commit -m "feat: improve logo, court records, AI titles and humor"
          git push origin HEAD:${{ github.head_ref }}
