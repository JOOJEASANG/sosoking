{
  "scripts": {
    "build": "node -e \"console.log('Static Firebase Hosting app: no build step')\"",
    "deploy": "npx firebase-tools deploy",
    "deploy:hosting": "npx firebase-tools deploy --only hosting",
    "deploy:functions": "npx firebase-tools deploy --only functions",
    "deploy:rules": "npx firebase-tools deploy --only firestore:rules,storage:rules",
    "deploy:indexes": "npx firebase-tools deploy --only firestore:indexes"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
